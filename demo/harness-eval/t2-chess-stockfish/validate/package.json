{
  "name": "t2-chess-stockfish-validate",
  "private": true,
  "type": "module",
  "description": "T2 gates: the artifact's move generator cross-examined by Stockfish (perft-1 oracle) under headless Chromium.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "validate": "node validate.mjs"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "stockfish": "^16.0.0"
  }
}
