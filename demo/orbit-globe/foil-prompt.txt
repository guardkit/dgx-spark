Build a single-file, browser-only whole-sky satellite tracker as one index.html — no build step, no API keys, no backend. An interactive 3D Earth shows every active satellite as a live, propagated point cloud — Starlink, GPS, the lot — with the ISS highlighted and labelled, a live lat/lng/altitude readout for it, each satellite's name and orbit path (±45 min) shown on click or hover, and time-warp controls (1×/60×/600×) with a HUD showing sim time and satellite count.

Fetch the TLE catalog once from https://celestrak.org/NORAD/elements/gp.php?GROUP=active&FORMAT=tle and compute all positions with in-browser SGP4 (satellite.js) — do not poll a position API for motion (https://api.wheretheiss.at/v1/satellites/25544 may be used for a one-time cross-check at most; never open-notify.org, it is HTTP-only and dies to mixed-content blocking). Libraries and globe textures only from cdn.jsdelivr.net or unpkg.com, pinned versions. Thousands of satellites must render at interactive frame rates; filter decayed/invalid elements instead of crashing. If the TLE fetch fails, show a visible error state, never a blank globe. Zero console errors. Make sure the ISS placement is accurate to its actual current coordinates.

Also expose this status object on the page, updated every frame (the sim must boot at 1× real time):

window.__orbitGlobe = {
  satCount: 0,
  simTime: Date.now(),
  timeWarp: 1,
  iss: { lat: 0, lng: 0, altKm: 0 },
  errors: []
};

Reply with ONLY the complete contents of index.html.
